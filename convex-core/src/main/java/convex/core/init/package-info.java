/**
 * Genesis state construction for new Convex networks.
 *
 * <p>Builds the initial global {@code State}: founding accounts, peer set,
 * core libraries (such as {@code convex.fungible} and {@code convex.asset}),
 * CNS registry, and governance actors. Used when bootstrapping test networks,
 * local peers, and new production networks.</p>
 */
package convex.core.init;