/**
 * General-purpose data lattice — a top-level lattice for storing arbitrary
 * application data with structured merge semantics.
 *
 * <p>Wraps lower-level lattice primitives to provide a convenient entry point
 * for applications that need a shared, convergent data space without building
 * their own lattice composition.</p>
 */
package convex.lattice.data;
