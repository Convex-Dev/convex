/**
 * Key-value lattice data structures — maps, sets, sorted sets, lists,
 * counters, and hash-based stores with convergent merge semantics.
 *
 * <p>These are the lattice analogues of common collection types, suitable for
 * building replicated indexes, caches, and application state that reconciles
 * automatically across nodes.</p>
 */
package convex.lattice.kv;
