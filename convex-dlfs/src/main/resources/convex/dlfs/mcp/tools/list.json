{
  "name": "dlfs_list",
  "description": "List directory contents in a DLFS drive. Returns an array of entries, each with name, type ('file' or 'directory'), and size (for files). Omit path to list the drive root.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "drive": {
        "type": "string",
        "description": "Drive name"
      },
      "path": {
        "type": "string",
        "description": "Directory path within the drive (e.g. 'docs/specs'). Omit for root directory."
      }
    },
    "required": ["drive"]
  }
}
