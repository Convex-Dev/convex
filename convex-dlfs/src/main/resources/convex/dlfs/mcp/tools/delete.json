{
  "name": "dlfs_delete",
  "description": "Delete a file or empty directory from a DLFS drive.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "drive": {
        "type": "string",
        "description": "Drive name"
      },
      "path": {
        "type": "string",
        "description": "Path of the file or directory to delete"
      }
    },
    "required": ["drive", "path"]
  }
}
