{
  "name": "dlfs_read",
  "description": "Read the contents of a file from a DLFS drive. Returns text content directly, or base64-encoded content for binary files.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "drive": {
        "type": "string",
        "description": "Drive name"
      },
      "path": {
        "type": "string",
        "description": "File path within the drive"
      }
    },
    "required": ["drive", "path"]
  }
}
