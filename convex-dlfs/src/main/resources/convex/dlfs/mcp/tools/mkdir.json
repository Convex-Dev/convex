{
  "name": "dlfs_mkdir",
  "description": "Create a directory in a DLFS drive.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "drive": {
        "type": "string",
        "description": "Drive name"
      },
      "path": {
        "type": "string",
        "description": "Directory path to create"
      }
    },
    "required": ["drive", "path"]
  }
}
