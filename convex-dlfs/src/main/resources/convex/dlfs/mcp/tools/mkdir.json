{
  "name": "dlfs_mkdir",
  "description": "Create a directory in a DLFS drive. The parent directory must already exist. To create nested paths, create each level in order (e.g. 'docs' then 'docs/specs').",
  "inputSchema": {
    "type": "object",
    "properties": {
      "drive": {
        "type": "string",
        "description": "Drive name, or a DID-URL reference to another user's drive (e.g. 'did:key:zOwner.../drive')"
      },
      "path": {
        "type": "string",
        "description": "Directory path to create (e.g. 'docs/specs')"
      },
      "ucans": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Optional UCAN JWT proofs authorising delegated access to another user's drive"
      }
    },
    "required": ["drive", "path"]
  }
}
