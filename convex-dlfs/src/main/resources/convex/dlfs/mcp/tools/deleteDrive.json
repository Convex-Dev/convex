{
  "name": "dlfs_delete_drive",
  "description": "Delete a DLFS drive and all its contents. This is irreversible — all files and directories in the drive are lost.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the drive to delete"
      }
    },
    "required": ["name"]
  }
}
