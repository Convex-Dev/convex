{
  "name": "transact",
  "title": "Convex Transaction",
  "description": "Submit a Convex transaction using the server's configured keypair",
  "inputSchema": {
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "description": "CVM source code to submit as a transaction '(transfer #13 10000000000)'"
      }
    },
    "required": ["source"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "value": {"description": "Resulting CVM value"},
      "errorCode": {"description": "CVM error code if the transaction failed"},
      "info": {"description": "Additional execution metadata"},
      "message": {"type": "string", "description": "Optional human-readable message"}
    }
  }
}

