{
  "name": "signingListAccounts",
  "title": "List Signing Accounts",
  "description": "Lists the signing keys for the authenticated identity. Returns entries with the public key for each stored key. On-chain address resolution will be added when peers have a key-to-account index.\n\nRequires authentication via bearer token.",
  "inputSchema": {
    "type": "object",
    "properties": {}
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "accounts": {
        "type": "array",
        "description": "Array of entries with publicKey and addresses[] for each signing key"
      }
    }
  }
}
