{
  "name": "query",
  "title": "Convex Query",
  "description": "Execute a Convex Lisp query. Queries are fast, free of fees, and do not affect the global state.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "description": "CVM source code to execute, e.g. '(balance #13)'"
      },
      "address": {
        "type": "string",
        "description": "Optional account address to use for the query, e.g. '#13'"
      }
    },
    "required": ["source"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "value": {
        "description": "Resulting CVM value"
      },
      "errorCode": {
        "description": "CVM error code if the query failed"
      },
      "info": {
        "description": "Additional execution metadata"
      },
      "message": {
        "type": "string",
        "description": "Optional human-readable message"
      }
    }
  }
}

