---
name: convex-db
description: Use Convex DB — a lattice-backed SQL database. Use when helping users write queries, connect via JDBC or PostgreSQL clients, create tables, insert/query data, or use the direct lattice API.
---

# Shared Convex skill

This is the Codex discovery entry for the canonical shared skill at
[`.claude/skills/convex-db/SKILL.md`](../../../.claude/skills/convex-db/SKILL.md).

Read that canonical `SKILL.md` completely, then follow its instructions. Resolve
all relative references from the canonical skill's directory. Keep workflow
instructions in the canonical file; do not duplicate or amend them here.
