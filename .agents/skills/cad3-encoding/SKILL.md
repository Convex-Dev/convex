---
name: cad3-encoding
description: CAD3 encoding format — cells, embedded vs branch references, value IDs, validity rules. Use when working on encoders, decoders, serialisation, hashing or anything that reads or writes cell encodings.
---

# Shared Convex skill

This is the Codex discovery entry for the canonical shared skill at
[`.claude/skills/cad3-encoding/SKILL.md`](../../../.claude/skills/cad3-encoding/SKILL.md).

Read that canonical `SKILL.md` completely, then follow its instructions. Resolve
all relative references from the canonical skill's directory. Keep workflow
instructions in the canonical file; do not duplicate or amend them here.
