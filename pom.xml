<project xmlns="https://maven.apache.org/POM/4.0.0"
	xmlns:xsi="https://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>world.convex</groupId>
	<artifactId>convex</artifactId>
	<version>0.8.12-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>Convex Parent</name>
	<description>Parent POM used to build core Convex modules.</description>
	<url>https://convex.world</url>

	<modules>
		<module>convex-core</module>
		<module>convex-cli</module>
		<module>convex-gui</module>
		<module>convex-peer</module>
		<module>convex-db</module>
		<module>convex-benchmarks</module>
		<module>convex-observer</module>
		<module>convex-java</module>
		<module>convex-x402</module>
		<module>convex-restapi</module>
		<module>convex-dlfs</module>
		<module>convex-social</module>
		<module>convex-p2p</module>
		<module>convex-integration</module>
	</modules>

	<properties>
		<!-- Bytecode target = the consumer floor: 21-targeted classes run on any JRE 21+,
		     keeping convex-java and the other library modules usable by downstream apps on
		     the current LTS. We develop and release on JDK 25, while CI verifies both JDK 21
		     and JDK 25. This target is independent from the build and test JDK. -->
		<maven.compiler.release>21</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<logback.version>1.6.0</logback.version>
		<jmh.version>1.37</jmh.version>
		<junit.version>6.1.2</junit.version>
		<slf4j.version>2.0.18</slf4j.version>
		<hc.version>5.5</hc.version>
		<netty.version>4.2.16.Final</netty.version>
		<convex.version>${project.version}</convex.version>
		<!--Freeze this for reproducible builds. -->
		<project.build.outputTimestamp>2026-08-10T12:20:31Z</project.build.outputTimestamp>
		<!-- slf4j.internal.verbosity=ERROR silences SLF4J's informational init-replay
		     notice in test output (at the cost of hiding e.g. multiple-provider warnings). -->
		<argLine>
			--add-opens=java.base/java.util=ALL-UNNAMED
			-Dslf4j.internal.verbosity=ERROR
		</argLine>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-lang3</artifactId>
				<version>3.20.0</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<licenses>
		<license>
			<name>Convex Public License</name>
			<url>https://github.com/Convex-Dev/convex/blob/master/LICENSE.md</url>
		</license>
	</licenses>

	<developers>
		<developer>
			<name>Mike Anderson</name>
			<email>mike@convex.world</email>
			<organization>Convex Foundation</organization>
			<organizationUrl>https://convex.world</organizationUrl>
		</developer>
	</developers>

	<scm>
		<connection>scm:git:git://github.com/Convex-Dev/convex.git</connection>
		<developerConnection>scm:git:ssh://github.com:Convex-Dev/convex.git</developerConnection>
		<url>https://github.com/Convex-Dev/convex.git</url>
	</scm>

	<distributionManagement>
		<snapshotRepository>
			<id>central</id>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
		</snapshotRepository>
	</distributionManagement>

	<profiles>
		<profile>
			<id>release</id>
			<activation>
				<property>
					<name>performRelease</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<plugins>
					<!-- This adds GPG signing for all artifacts -->
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.2.8</version>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
					</plugin>


				</plugins>
			</build>
		</profile>
	</profiles>

	<build>
		<plugins>
			<!-- Use this to require a minimum Maven version -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>3.6.3</version>
				<executions>
					<execution>
						<id>enforce-maven</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>3.7</version>
								</requireMavenVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			
			<!-- Actually activate the spurce / javadoc plugins for every module -->
		    <plugin>
		      <artifactId>maven-source-plugin</artifactId>
		      <executions><execution><id>attach-sources</id></execution></executions>
		    </plugin>
		    <plugin>
		      <artifactId>maven-javadoc-plugin</artifactId>
		      <executions><execution><id>attach-javadocs</id></execution></executions>
		    </plugin>

		    <plugin>
  		        <groupId>org.sonatype.central</groupId>
				<artifactId>central-publishing-maven-plugin</artifactId>
				<version>0.11.0</version>
 				<extensions>true</extensions>
				<configuration>
					<publishingServerId>central</publishingServerId>
					<checksums>all</checksums>
					<!-- <waitUntil>published</waitUntil> -->
					<autoPublish>true</autoPublish> 
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.cyclonedx</groupId>
				<artifactId>cyclonedx-maven-plugin</artifactId>
				<version>2.9.2</version>
				<executions>
					<execution>
						<phase>deploy</phase>
						<goals>
							<goal>makeAggregateBom</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<projectType>library</projectType>
					<schemaVersion>1.4</schemaVersion>
					<includeBomSerialNumber>true</includeBomSerialNumber>
					<includeCompileScope>true</includeCompileScope>
					<includeProvidedScope>true</includeProvidedScope>
					<includeRuntimeScope>true</includeRuntimeScope>
					<includeSystemScope>true</includeSystemScope>
					<includeTestScope>false</includeTestScope>
					<includeLicenseText>false</includeLicenseText>
					<outputReactorProjects>true</outputReactorProjects>
					<outputFormat>json</outputFormat>
					<outputName>CycloneDX-Sbom</outputName>
					<verbose>false</verbose>
				</configuration>
			</plugin>
		</plugins>
		
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-jar-plugin</artifactId>
					<version>3.5.1</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.15.0</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>3.5.6</version>
					<configuration>
						<forkedProcessExitTimeoutInSeconds>120</forkedProcessExitTimeoutInSeconds>
					</configuration>
				</plugin>
				<plugin>
                	<groupId>org.apache.maven.plugins</groupId>
                	<artifactId>maven-resources-plugin</artifactId>
                	<version>3.5.0</version>
                	<configuration>
				        <!-- Silence warnings with explicit encoding -->
				        <encoding>UTF-8</encoding>
				        <propertiesEncoding>UTF-8</propertiesEncoding>
				    </configuration>
            	</plugin>

				<!-- This adds sources -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-source-plugin</artifactId>
					<version>3.4.0</version>
					<executions>
						<execution>
							<id>attach-sources</id>
							<goals>
								<goal>jar-no-fork</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
				
			

				<!-- This generates JavaDocs -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-javadoc-plugin</artifactId>
					<version>3.12.0</version>
					<configuration>
						<doclint>syntax</doclint>
						<quiet />
						<doctitle>${project.name} ${project.version} API</doctitle>
						<windowtitle>${project.name} ${project.version} API</windowtitle>
						<header><![CDATA[<b>Convex</b> ${project.version}]]></header>
						<bottom><![CDATA[Copyright &copy; Convex Foundation. Licensed under the <a href="https://github.com/Convex-Dev/convex/blob/master/LICENSE.md">Convex Public License</a>. See <a href="https://docs.convex.world/">docs.convex.world</a> for user documentation.]]></bottom>
						<notimestamp>true</notimestamp>
					</configuration>
					<executions>
						<execution>
							<id>attach-javadocs</id>
							<goals>
								<goal>jar</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>
