(do 
  (set-holding *address* {:name "Registry Contract"})
  
  (defn register [data]
    (set-holding *caller* data))
    
  (defn lookup [addr]
    (get-holding addr))
    
  (export register lookup))